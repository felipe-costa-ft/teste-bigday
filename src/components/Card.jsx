import styled from "styled-components";

const Card = styled.div`
  background-color: white;
  width: fit-content;
  padding: 2em 0.5em;
  border-radius: 10px;
  margin: 1em auto;
`;

export default Card;
